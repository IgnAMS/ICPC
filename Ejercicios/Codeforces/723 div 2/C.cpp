#include<bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<n;i++)
#define repx(i,a,b) for(int i=a;i<n;i++)
typedef long long ll;
typedef pair<ll, ll> ii;

int main(){
    ios::sync_with_stdio(0); cin.tie(0);
    int n; cin>>n;
    ll vida = 0;
    vector<ii> neg;
    vector<ll> acc(n + 1, 0);
    vector<ll> A(n); rep(i,n) { cin>>A[i]; neg.push_back({A[i], i}); }
    






    return 0;
}