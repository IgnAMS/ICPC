#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i,n) for(int i=0;i<n;i++) 
typedef pair<int,int> ii;
#define ff first
#define ss second

ll lineas[6];
vector<ll> cuerdas;
vector<vector<ii>> DP;
int n;
ii dp(int pos, int cuerda){
    if(pos==n) ;
    if()
    
    rep(j,6)
        DP[pos][i].ff = min(dp(pos+1,))
    
}



int main(){
    rep(i,6) cin>>linea[i];
    cin>>n;
    cuerdas.resize(n);
    rep(i,n) cin>>cuerdas[i];
    DP.assign(n+3,vector<int>(6,{-1,-1}));
    


    return 0;
}