#include<bits/stdc++.h>
using namespace std;
int main(){
  long long a,b;
  //vector<long long> ans;
  while(cin>>a>>b){
    //if(a==0 && b==0) break;
    //ans.push_back(abs(a-b));
    cout<<abs(a-b)<<endl;
  }

  return 0;
}
